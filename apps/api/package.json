{
  "name": "@proply/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "lsof -ti:3000 | xargs kill -9 2>/dev/null; node --env-file=../../.env --watch src/index.mjs",
    "start": "node --env-file=../../.env src/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "echo 'no ts in api — skipping'"
  },
  "dependencies": {
    "@proply/core": "workspace:*",
    "express": "^4.19.0",
    "cors": "^2.8.5"
  }
}
