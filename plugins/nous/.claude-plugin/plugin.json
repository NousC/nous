{
  "$schema": "https://code.claude.com/schemas/claude-plugin.json",
  "name": "nous",
  "version": "0.1.0",
  "description": "Nous — GTM data infrastructure for agents. One MCP call returns the full identity-resolved Account Record.",
  "author": {
    "name": "Nous",
    "url": "https://opennous.cloud"
  },
  "homepage": "https://opennous.cloud",
  "repository": "https://github.com/NousC/nous",
  "license": "AGPL-3.0",
  "keywords": ["mcp", "gtm", "crm", "memory", "agents", "context"],
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Nous API key (optional)",
      "description": "Leave blank and run /nous-login to sign in with your browser — no copy-paste. Or paste a key from https://opennous.cloud → Settings → API Keys. The key is workspace-scoped.",
      "sensitive": true,
      "required": false
    }
  },
  "mcpServers": {
    "nous": {
      "command": "npx",
      "args": ["-y", "@opennous/mcp"],
      "env": {
        "NOUS_API_KEY": "${user_config.api_key}"
      }
    }
  }
}
